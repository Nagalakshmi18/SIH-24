// ignore_for_file: file_names

import 'package:flutter/material.dart';

class ArrestedPersons extends StatelessWidget {
  const ArrestedPersons({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Arrested Persons'),
      ),
      body: const Center(
        child: Text(' Arrested Persons Page'),
      ),
    );
  }
}
