// ignore_for_file: file_names

import 'package:flutter/material.dart';

class EPaymentChallans extends StatelessWidget {
  const EPaymentChallans({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('E-Payment for Traffic Challans'),
      ),
      body: const Center(
        child: Text('Epayment Challans Page'),
      ),
    );
  }
}
